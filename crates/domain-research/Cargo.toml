[package]
name = "domain-research"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true

[lib]
name = "domain_research"
path = "src/lib.rs"
crate-type = ["rlib", "cdylib"]

[dependencies]
domain-common = { path = "../domain-common" }
graph-core = { path = "../graph-core" }
domain-provider-abi = { path = "../domain-provider-abi" }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
# Cross-domain composition contract: a clinical trial validated by both packs
# must round-trip as one record.
domain-medical = { path = "../domain-medical" }

[lints]
workspace = true
